// A function has been provided for you, it accept's two arguments, an input and a char,
// the input being the users input and the char being the character to remove from the input. 
// The function should also check if the input or char is valid, no empty values or numbers are allowed, 
// should return an empty string if the input or char is invalid

const parseInput = (input,char) => {
  // Write your code here

}

module.exports = parseInput